{
  "name": "latentcreate",
  "private": true,
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "Open-source desktop app for AI music creation",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "app:dev": "npm run dev --prefix app",
    "app:build": "npm run build --prefix app",
    "app:test": "npm run test --prefix app",
    "app:lint": "npm run lint --prefix app",
    "gate": "npm run gate:rust && npm run gate:app",
    "gate:rust": "cargo fmt --all --check && cargo clippy --all-targets -- -D warnings && cargo test --workspace",
    "gate:app": "npm run gate --prefix app"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.3"
  }
}
