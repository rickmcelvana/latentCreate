[package]
name = "mcp-bridge"
description = "MCP client for a local ComfyUI via `comfy-mcp`."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[features]
# The in-memory fake peer, for offline tests in this crate and in the crates
# that drive it. A dev-dependency only: `cargo build` never sees it, so no
# mock reaches a shipped binary.
test-support = []

[dependencies]
rmcp = { version = "3.1.4", default-features = false, features = ["client", "transport-child-process"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
tokio = { version = "1.53", features = ["process", "rt", "time", "io-util"] }

[dev-dependencies]
tempfile = "3"
tokio = { version = "1.53", features = ["macros", "io-util"] }
